export const dispensariesPromptModalTypes: {[key: string]: string} = {
  default: '',
  emptyCart: 'emptyCart',
};

export const locationsPromptModalTypes: {[key: string]: string} = {
  default: '',
  emptyCart: 'emptyCart',
};
